package uk.emarte.regurgitator.extensions.web;

import uk.emarte.regurgitator.test.Mock;

public class HttpMessageProxy extends Mock {
    public HttpMessageProxy(HttpClientWrapper wrapper) {
        super(wrapper);
    }
}
