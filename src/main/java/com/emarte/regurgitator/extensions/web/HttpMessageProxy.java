package com.emarte.regurgitator.extensions.web;

import com.emarte.regurgitator.test.Mock;

public class HttpMessageProxy extends Mock {
	public HttpMessageProxy(String host, int port) {
		super(host, port);
	}
}
