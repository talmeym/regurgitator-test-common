package com.emarte.regurgitator.extensions;

import com.emarte.regurgitator.core.ContextLocation;
import com.emarte.regurgitator.test.Mock;

public class FreemarkerBuilder extends Mock {
	public FreemarkerBuilder(ContextLocation source, String staticValue) {
		super(source, staticValue);
	}
}
